import React from 'react';
import { StyledDescription } from '../styled/mainContent';

export default () => {
  return (
    <StyledDescription>
      i am a frontend developer, always eager to learn and make stuff better, let's make something great.
    </StyledDescription>
  );
}